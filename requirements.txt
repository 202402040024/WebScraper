# Core — always installed
streamlit>=1.30.0,<2.0.0
pymongo>=4.6.0
pytesseract>=0.3.10
pillow>=10.0.0
pandas>=2.0.0
plotly>=5.15.0
requests>=2.31.0
beautifulsoup4>=4.12.0
fake-useragent>=1.1.3
python-dotenv>=1.0.0
openpyxl>=3.1.0
lxml>=4.9.0

# Browser-based scrapers (used locally; Render falls back to BS4)
selenium>=4.10.0
webdriver-manager>=4.0.1
playwright>=1.35.0

# Scrapy (subprocess-based, works on Render)
scrapy>=2.9.0

# API server (optional)
fastapi>=0.100.0
uvicorn>=0.22.0
